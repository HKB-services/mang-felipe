import OrderPage from "@/features/orders/pages/OrderPage"

// Menu availability is admin-controlled and must not be frozen at build time.
export const dynamic = "force-dynamic"

export default OrderPage
